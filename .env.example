# Supabase 配置
# 从 https://app.supabase.com/project/_/settings/api 获取这些值

# Supabase 项目 URL
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url

# Supabase 匿名密钥（公开密钥，可以安全地在客户端使用）
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# 跨域认证配置
# 允许的域名白名单，用逗号分隔（服务端使用）
ALLOWED_ORIGINS=http://localhost:3000,https://app.lacs.cc

# 客户端跨域认证配置
# 允许的域名白名单，用逗号分隔（客户端使用）
NEXT_PUBLIC_ALLOWED_ORIGINS=http://localhost:3000,https://app.lacs.cc

# JWT 密钥（用于生成安全令牌）
JWT_SECRET=your-jwt-secret-key-here
